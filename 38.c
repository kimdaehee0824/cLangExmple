#include <stdio.h>
void Trans(char res[], int N, int B);
int main()
{
    char res[35] = {0};
    int num, B;
    scanf("%d %d", &num, &B);
    Trans(res, num, B);
    printf("%s\n", res);
    return 0;
}
void Trans(char res[], int N, int B)
{
    // 작성하세요!
}